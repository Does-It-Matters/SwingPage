package org.example.page.task.general.user.signup

internal class Request {
    //    private final ObjectMapper objectMapper = new ObjectMapper();
    fun signUp(user: EntityGeneralUser?) {
        println("url " + user)

    }
}
